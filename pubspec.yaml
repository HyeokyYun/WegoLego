name: livq
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.2.8

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^3.3.4
  firebase_core: ^1.10.6
  google_sign_in: ^5.2.1
  sign_button: ^2.0.2
  get: ^4.6.1
  google_fonts: ^2.1.0
  cloud_firestore: ^3.1.5
  flutter_svg: ^1.0.0
  firebase_messaging:
  http:
  agora_rtc_engine: ^4.0.7
  permission_handler:
  favorite_button:
  flutter_local_notifications:
  shared_preferences: ^2.0.11
  sign_in_with_apple: ^3.3.0
  youtube_player_flutter: ^8.0.0
  flutter_screenutil:
  animated_splash_screen: ^1.1.0
  font_awesome_flutter:
  flutter_swiper_null_safety:
  animated_bottom_navigation_bar: ^0.3.2
  circular_countdown_timer:
  firestore_search: ^0.1.9
  cupertino_icons: ^1.0.2
  url_launcher: ^6.0.18
  image_picker: ^0.8.4+5
  firebase_storage: ^10.2.6
  in_app_review: ^2.0.4

  flutter_speed_dial: ^5.0.0+1
  flutter_switch: ^0.3.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/liveQ_logo.svg
    - assets/app_bar/appBar.svg
    - assets/app_bar/bottomIcon1.svg
    - assets/app_bar/bottomIcon2.svg
    - assets/home/1.png
    - assets/home/2.png
    - assets/home/3.png
    - assets/home/ask_button.png
    - assets/home/icon1.svg
    - assets/home/icon1Push.svg
    - assets/home/icon2.svg
    - assets/home/icons.png
    - assets/home/thank_check.svg
    - assets/home/Vector.png
    - assets/home/speaker.png
    - assets/login/google.svg
    - assets/my_page/heartIcon.svg
    - assets/my_page/answerIcon.svg
    - assets/my_page/profile.svg
    - assets/my_page/questionIcon.svg
    - assets/my_page/yellow_i.png
    - assets/my_page/star_icon.png
    - assets/splash_page/LiveQ_logo.gif
    - assets/splash_page/splash_text.svg
    - assets/welcome_page/img1.svg
    - assets/welcome_page/img2.svg
    - assets/welcome_page/img3.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
